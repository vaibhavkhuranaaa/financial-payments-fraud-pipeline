# Workbench configuration. Raw data and artifacts remain gitignored.
FRAUD_ARTIFACT_ROOT=artifacts
DASHBOARD_PORT=8050
# Disabled by default for local use. The public service sets both values.
PUBLIC_RATE_LIMIT_RPS=0
PUBLIC_RATE_LIMIT_BURST=40
